sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t imhjnoh/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true imhjnoh/docker-react-app npm run test -- --coverage

deploy:
  provider: elasticbeanstalk
  region: "ap-northeast-2"
  app: "docker-react-app"
  env: "Dockerreactapp-env"
  bucket_name: "elasticbeanstalk-ap-northeast-2-089801491560"
  bucket_path: "docker-react-app"
  on:
    branch: master
    access_key_id: $AWS_ACCCESS_KEY
    secret_access_key: $AWS_SECRET_ACCESS_KEY
